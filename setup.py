from setuptools import setup,find_packages

setup(
    name='src',
    version="0.0.1",
    description="it is wine package",
    author='ajsal',
    packages=find_packages(),
    license='MIT'
)